import React from 'react';

interface PWAInstallPromptProps {
  variant?: 'banner' | 'button' | 'full';
  className?: string;
}

export const PWAInstallPrompt: React.FC<PWAInstallPromptProps> = () => {
  return null;
};
